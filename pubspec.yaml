name: countdown_ring
description: >-
  A circular countdown ring widget, a wall-clock timer model that survives
  backgrounding and process death, and compact duration formatting helpers.
version: 0.1.0
repository: https://github.com/JacobStephens2/countdown_ring
issue_tracker: https://github.com/JacobStephens2/countdown_ring/issues

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
